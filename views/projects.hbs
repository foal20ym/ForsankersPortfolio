<h1>Projects list!</h1>

{{#if projects.length}}
    <ul>
        {{#each projects}}
            <li><a href="/projects/{{id}}">{{title}}</a> {{grade}}/10</li>
        {{/each}}
    </ul>

    {{else}}
    <p> There is currently no projects to display. </p>
{{/if}}

<h1>Update Comment</h1>
{{#if errorMessages.length}}
<p>Couldn't update the comment because:</p>
<ul>
    {{#each errorMessages}}
    <li>{{this}}</li>
    {{/each}}
</ul>
{{/if}}
<form method="POST">
    <div>
        <input type="text" name="comment" value="{{comment.comment}}">
    </div>
    <input type="submit" value="Update Comment">
</form>